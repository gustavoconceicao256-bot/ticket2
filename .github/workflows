name: Render Deploy Hook

on:
  schedule:
    - cron: "0 */3 * * *"

  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Chamar Deploy Hook do Render
        run: curl -X GET "${{ secrets.RENDER_DEPLOY_HOOK }}"
